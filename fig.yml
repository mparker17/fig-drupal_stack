# MySQL.
db:
  image: stackbrew/mysql
  ports:
    - "3306:3006"
  environment:
    - MYSQL_ROOT_PASSWORD=root

# Apache / PHP.
http:
  image: "stackbrew/php:5.6-apache"
  ports:
    - "80:80"
  links:
    - db
  volumes:
    - webroot:/var/www/html
